# W1-06A Shadow Aggregate Audit — Implementation Status

## Authorization and branch

```text
base branch: WorldWright-new
base commit: ff71a9fea348c95a442f1ccb8f34c60dd243314b
implementation branch: agent/w1-06a-shadow-audit-aggregate-harness
W1-05B: merged through PR #142
physical generator authority: LEGACY
causal authority: CAUSAL_SHADOW only
ordinary Generate integration: forbidden
visible physical output changes: forbidden
CAUSAL_ACTIVE: forbidden and unimplemented
merge authorization: NOT GRANTED
```

## Purpose

W1-06A establishes the first complete-corpus aggregate diagnostic gate over the detached regime-history and geologic-spine chain.

It adds:

```text
28 committed unique-seed cases
7 controlled baseline families
aggregate branch and graph distributions
determinism counts
open contradiction counts
frozen initial runtime, memory, payload, and graph budgets
explicit coverage-gap reporting
an independent GitHub Actions artifact gate
```

## Scientific status

The software gate may pass while the scientific status remains `PARTIAL`.

W1-06A intentionally reports missing coverage for:

```text
remaining controlled archetype families
remaining direct-input threshold axes
negative references
approved exception references
full premise/interior direct-input execution
```

These gaps are not converted into passing evidence.

## Authority boundary

The aggregate audit:

```text
reads detached immutable causal records
writes diagnostic artifacts only
never receives or writes WorldBrain
never writes terrain, elevation, sea level, bathymetry, climate, hydrology, biomes, materials, resources, or rendering
never changes LEGACY generation
never promotes causal physical authority
```

Operational timing and heap measurements are explicitly diagnostic and never enter causal identity.

## Current corpus

The initial 28 cases replay four unique seeds across each of seven validated baseline families:

```text
stagnant-lid rocky
mobile-or-episodic rocky
high-heat young super-Earth
tidally heated rocky threshold
tidal ice shell
rock-ice mixed holdout
volatile-pressure holdout
```

The upstream premise and interior records remain controlled validated fixtures in W1-06A. This provides a stable aggregate gate without falsely claiming that the complete direct-input threshold matrix already exists.

## Required validation

```text
build succeeds
full test suite succeeds
28-case aggregate gate succeeds
at least 24 unique seeds
at least 7 distinct history hashes
28 distinct spine hashes
zero blocked or failed aggregate cases
frozen performance and graph budgets pass
required diagnostics succeed
legacy snapshot canary succeeds
384×192 full-globe capture succeeds
visible legacy output remains unchanged
```

## Next bounded scope

After W1-06A is exact-head green and separately approved for merge, W1-06B may add the missing controlled archetype families, direct-input threshold matrix, and geology-reference integration.

W1-06A does not authorize W1-06B merge, W1-07, process-field rasterization, causal physical authority, `CAUSAL_ACTIVE`, or visible planet changes.
