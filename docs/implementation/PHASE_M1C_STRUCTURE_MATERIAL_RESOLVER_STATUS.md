# M1C Detached Structure/Material Resolver — Status

## Governing boundary

```text
base branch: WorldWright-new
base commit: 1754a105a417b322743ad9da481ee3062ff0ea83
implementation branch: agent/m1c-detached-structure-material-resolver
authority mode: CAUSAL_SHADOW
physical generator authority: LEGACY
scientific status: PARTIAL
ordinary Generate change: none
visible physical-output change: none
CAUSAL_ACTIVE: unimplemented and forbidden
threshold calibration: forbidden
```

M1C implements the first detached source-backed structure/material province resolver. It consumes the reviewed M1B rules and fixed corpus, builds immutable partial candidates, preserves unresolved and ambiguous states, and remains entirely outside ordinary Generate.

## Resolver behavior

The resolver requires:

```text
reviewed M1B rule and fixture package
separate M1C implementation authorization
natural solid-body premise candidates
upstream structural-role candidates
continuous Phase D process-field values
real contributing geologic-spine source families
exact premise, history, spine, projection, and structural-interpretation lineage
```

Candidate construction is relational and source-gated. It does not introduce universal normalized thresholds. Supported rules may produce partial candidates. `EXHUMED_MANTLE_TRANSITION` and `MIXED_TRANSITIONAL_PROVINCE` may appear only as non-leading ambiguity alternatives. `STRUCTURE_MATERIAL_UNRESOLVED` remains the explicit fail-closed result.

## Fixed-corpus behavior

```text
positive: 6
threshold/boundary: 6
negative: 3
exception: 1
withheld holdout: 2
total: 18
```

The complete M1B corpus is exercised by the M1C resolver. Holdouts remain withheld from calibration. The resolver uses rule eligibility, structural context, source lineage, and relative evidence relations rather than fitting generated-world frequencies.

## Authority firewall

M1C cannot read or write:

```text
legacy solved morphology as causal evidence
surfaceExposureSummary as deep material evidence
direct initial-condition inputs as material-class shortcuts
base terrain
final terrain
bathymetry
sea level
land or water state
surface material or exposed rock
renderer or presentation state
ordinary Generate output
```

The state keeps all of the following false:

```text
structureMaterialCauseAuthority
landformPotentialAuthority
baseTerrainAuthority
surfaceMaterialAuthority
finalLandAuthority
finalWaterAuthority
bathymetryAuthority
terrainAuthority
```

Terrain-term permissions remain non-authoritative candidate labels only.

## Acceptance gates

The exact M1C head must pass:

```text
complete 18-case M1C resolver corpus
withheld holdout validation
live projection-to-structure integration
source-lineage rejection
surface and legacy-input hostile tests
research-required non-leading validation
M1C artifact and workflow tests
full Vitest suite
TypeScript build
required Generate diagnostics
seven-seed causal skeleton harness
seven-seed legacy physical-output equivalence
snapshot canary
full-globe review
all inherited Wave 1, Phase D, Phase C, M1A, and M1B gates
```

## Current verdict

```text
M1C detached resolver: implemented pending exact-head validation
M1B fixed corpus: consumed without calibration
research-required classes: non-leading
physical promotion: blocked
ordinary Generate integration: blocked
next phase entry: requires exact-head validation, review, merge, and separate explicit authorization
```
