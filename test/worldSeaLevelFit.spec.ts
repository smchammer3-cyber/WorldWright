import { describe, it } from 'vitest';

describe.skip('profile sea level fitting experiment', () => {
  it('was removed from the default generator path after PR39 failed visual validation', () => {
    // Kept as a placeholder so the failed experiment does not remain active.
  });
});
